# (h) `CLAUDE.md` + `.github/copilot-instructions.md` Эти два файла — указатели. Они не дублируют `AGENTS.md`, они ссылаются на него. Это сознательное решение: одно место правды, тонкие адаптеры под каждый инструмент. ## `CLAUDE.md` ```md # CLAUDE.md Claude Code: read `/AGENTS.md`first. That file is the single source of truth for agent behavior on this repo. ## Quick orientation (Claude-specific) - Output format: unified diff by default. New files: full content in fenced blocks with the path on line 1 as a comment. - One PR = one logical change. If the task spans multiple concerns, ask the user to split before starting. - Tools you should default to:`read`, `edit`, `bash`. Use `bash`only for commands listed in`.opencode/config.json` `tools.bash.allow`(the same allowlist applies to Claude Code). - Never`git push`, `git reset --hard`, `rm -rf`, or modify `.env\*`. - Never `pnpm add <package>` without an explicit user request that names the package and version. ## File precedence (re-stated)

AGENTS.md → docs/STACK-DECISION.md → prompts/META/INDEX.md → prompts/META/step-2-drizzle-ddl/README.md → prompts/META/step-3-implementations/\* → prompts/META/step-1-blocks/B\*_ → docs/_

When two sources disagree, the higher one wins. Quote both in the PR description and explain the choice.

## Forbidden actions (extracted from AGENTS.md for visibility) - Inventing file paths that don't exist. Stop and ask. - Using forbidden vocabulary (AGENTS.md §4). - Adding fields to `/verify-card` response beyond the 5 allowed (§5). - Touching files outside the prompt's "Files to add/modify" list. When in doubt: stop, ask, do not proceed.
